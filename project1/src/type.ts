export interface StudentItem {
    id: number
    name: string
    surname: string
    course: string
    image: string
}

export interface Advisor {
    name: string
    surname: string
    image: string
}